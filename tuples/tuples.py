# tuples are not changeable. Dont Forget !

thistuple = ("apple","Banana",  "Cherity")

print(thistuple[0]) # apple

tuple = (1,"iki",True) # tuples can include diffrent variables types
tuple2 = ("naber","gelmedi","senden",1,"Haber",True)
print(tuple) #(1, 'iki', True)

# u cant use append() method 

print(tuple.count(1)) # 2

print(tuple + tuple2) # (1, 'iki', True, 'naber', 'gelmedi', 'senden', 1, 'Haber', True)

# tuples are more usefull to lists because tuples are net abusable.
